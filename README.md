# Driver Behavior Detection (CNN+RNN) – Streamlit Web App

1) Put your trained model in `models/` (e.g., `models/seq_model.keras`).
2) Run: `pip install -r requirements.txt` then `streamlit run app.py`.
